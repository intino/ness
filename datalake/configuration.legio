dsl Legio

Project("io.intino.ness", "1.0.0") datalake
	Repositories
		Release("https://artifactory.intino.io/artifactory/releases", "intino-maven")
	Dependencies
		Compile(groupId = "io.intino.ness", artifactId = "inl-java8", version = "1.0.0")
		Compile(groupId = "io.intino.konos", artifactId = "konos-jms", version = "1.0.3")
		Test(groupId = "junit", artifactId = "junit", version = "4.12")
LifeCycle
	Package(LibrariesLinkedByManifest)
	Distribution
		Release(url = "https://artifactory.intino.io/artifactory/release-libraries", mavenId = "intino-maven")