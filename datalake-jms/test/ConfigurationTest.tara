dsl Ness

Tank("consul.processstatus") processstatus
Tank("consul.processlog") processlog
Tank("consul.devicecrash") devicecrash
Tank("consul.deviceupgrade") deviceupgrade
Tank("consul.deviceboot") deviceboot
Tank("consul.devicestatus") devicestatus
Tank("consul.serverupgrade") serverupgrade
Tank("consul.serverlog") serverlog
Tank("consul.serverstatus") serverstatus
Tank("consul.serverboot") serverboot
Tank("cesar.infrastructureoperation") infrastructureOperation

User(name = "test", password = "test")
