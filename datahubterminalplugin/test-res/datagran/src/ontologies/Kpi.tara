dsl Ness

Event Kpi
	Attribute timeScale as Word("Minute" "Hour" "Day" "Week" "Month" "Year")
	Attribute installation as Text
	Attribute place as Text
	Attribute min as Real
	Attribute mean as Real
	Attribute max as Real
	Attribute unit as Text

Event GridPower as ExtensionOf(Kpi)

Event GridEnergy as ExtensionOf(Kpi)

Event NegativeGridEnergy as ExtensionOf(Kpi)

Event ConsumedPower as ExtensionOf(Kpi)

Event ConsumedEnergy as ExtensionOf(Kpi)

Event PhotovoltaicPanelPower as ExtensionOf(Kpi)

Event PhotovoltaicPanelEnergy as ExtensionOf(Kpi)

Event Phi as ExtensionOf(Kpi)

Event GlobalIrradiance as ExtensionOf(Kpi)

//Event EfVerticalIrradiance as ExtensionOf(Kpi)
//
//Event EfAngledIrradiance as ExtensionOf(Kpi)

Event Temperature as ExtensionOf(Kpi)

Event RelativeHumidity as ExtensionOf(Kpi)

Event ResistanceTemperature as ExtensionOf(Kpi)

Event WindVelocity as ExtensionOf(Kpi)

Event WindDirection as ExtensionOf(Kpi)