dsl Legio

Artifact (groupId = "io.intino.ness", version = "1.0.5") inl
	Imports
		Test("junit", "junit", "4.12")

	Package(LibrariesLinkedByManifest) > attachSources = true
	Distribution(intino.releases, empty)

Repository("intino-maven") intino
	Release("https://artifactory.intino.io/artifactory/releases")
	Release("https://artifactory.intino.io/artifactory/release-libraries") releases